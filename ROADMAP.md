# atray 路线图（ROADMAP）

> 2026-09-06 记录 · 当前可用版本：**v1.2.0**（Windows + Linux KDE Wayland 热键全链路已验证）

## 版本定位总览

- **v1.x —— 打磨期**：在现有形态（单覆盖层 + 单活动 web 应用 + 热键切换）上打磨细节、
  修边角、补验证，**不引入大的形态变化**。当前 v1.2.0 即一个「可用」的稳定版。
- **v2.0.0 —— 场景功能**：引入「场景（Scene）」，与「单 web 应用」并列的顶层概念；
  一个场景内多个 web 应用**并行运行**。涉及窗口模型与配置模型升级。

## v1.x 打磨清单（候选，按实际痛点排列）

开发/联调中发现的真实边角，供 1.x 各小版本挑选：

1. **Windows 退出路径**：历史 panic.log 出现过 tao "cannot move state from Destroyed"
   （退出时崩溃）——复现并确认当前版本干净退出
2. **热键冲突检测**：总快捷键 / 各应用快捷键重复或互占时，设置页给出冲突提示
   （当前 native 后端注册失败的细节没有完整透出到 UI）
3. **双副本一致性**：Windows 运行副本与桌面副本（atray-desktop）易版本漂移——
   考虑「版本号显示 + 部署提醒」
4. **设置页体验**：显示应用版本号；config.json 损坏时的备份与恢复
5. **Linux 打磨**：portal 残留 cleanup 仅 KDE 下执行（GNOME 消除噪音日志）；
   无托盘桌面（GNOME 等）提供替代「退出」入口；.desktop + StartupWMClass
   系统图标注册（部分已验证：appIcon 替换已解决系统 H 图标问题）
6. **正式打包**：tauri bundle 未启用（bundle.active=false）——1.x 后期补
   Windows MSI/NSIS、Linux AppImage/deb
7. **跨平台回归核对**：多显示器 / DPI（Windows force-device-scale-factor=1）、
   开机自启行为（Linux .desktop vs Windows Run 键）、Alt+数字 切换
8. **GNOME / 其它 Wayland 实测**：portal 接口检查命令、绑定对话框、托盘扩展
   （KGA 为 KDE 专属通道；portal 为跨桌面备用）

## v2.0.0 —— 场景（Scene）

### 需求要点（2026-09-06 确认）

- 场景 = 一组 web 应用窗口**并行**运行的工作区；用户可按场景整体呼出/切换
- **场景与单 web 应用是并列的概念**：顶层可切换对象既可以是单个应用，也可以是
  一个场景（同级实体，不是层级归属）
- v1 形态（单覆盖层、单活动应用、Alt+数字 切换）保留为「单应用」语义

### 候选设计方向（2.0 设计阶段细化）

- **窗口模型**：并行 = 多 WebView 窗口平铺/分屏，还是覆盖层内多面板？需先定
- **配置模型**：config schema v2（scenes 列表：应用/热键/布局），v1 配置平滑迁移
- **快捷键模型**：场景级热键（一键拉起整场景）与现有应用级热键并存
- **平台差异**：Windows / Linux(KDE Wayland) 的并行窗口、全屏、托盘语义；
  Linux 多窗口下全局热键按窗口 or 按场景注册

### 里程碑建议

- **2.0 alpha**：配置模型 v2 + 场景管理 UI（CRUD / 排序 / 改名）
- **2.0 beta**：并行窗口/布局 + 场景热键切换
- **2.0.0**：持久化、双平台差异打磨、文档

## 版本速览

| 版本 | 内容 | 状态 |
|---|---|---|
| v1.0.0 | 从 anm-tauri 分出；覆盖层 / 设置 v2 / 布局 / 图标 | 历史 |
| v1.1.0 | Linux Wayland 支持（portal + KGA 雏形、显式全屏、残留清理） | 历史 |
| v1.2.0 | Linux 热键全链路打通（KGA SetPresent 根因修复）+ 对账 v2 + 细节打磨 | ✅ 当前可用 |
| v1.x | 打磨（见上表） | 规划中 |
| v2.0.0 | 场景（Scene）：多 web 应用窗口并行 | 规划中 |
