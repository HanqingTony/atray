# atray 路线图（ROADMAP）

> 2026-09-06 记录 · 当前可用版本：**v1.2.0**（Windows + Linux KDE Wayland 热键全链路已验证）
> 2026-09-06 修订：**2.0 优先做「快捷脚本」，场景（Scene）整体移到 3.0**。

## 版本定位总览

- **v1.x —— 打磨期**：在现有形态（单覆盖层 + 单活动 web 应用 + 热键切换）上打磨细节、
  修边角、补验证，**不引入大的形态变化**。当前 v1.2.0 即一个「可用」的稳定版。
- **v2.0.0 —— 快捷脚本**：除 web 应用外，可把**脚本绑定到快捷键**（一键执行外部
  命令/工具）。设置新增「我的脚本」。为「脚本将来挂到面板自定义按钮」预留架构。
- **v3.0.0 —— 场景（Scene）**：与「单 web 应用」并列的顶层概念；一个场景内多个
  web 应用**并行运行**（原 2.0 规划整体后移）。

## v1.x 打磨清单（候选，按实际痛点排列）

开发/联调中发现的真实边角，供 1.x 各小版本挑选：

1. **Windows 退出路径**：历史 panic.log 出现过 tao "cannot move state from Destroyed"
   （退出时崩溃）——复现并确认当前版本干净退出
2. **热键冲突检测**：总快捷键 / 各应用快捷键重复或互占时，设置页给出冲突提示
   （当前 native 后端注册失败的细节没有完整透出到 UI）
3. **双副本一致性**：Windows 运行副本与桌面副本（atray-desktop）易版本漂移——
   考虑「版本号显示 + 部署提醒」
4. **设置页体验**：显示应用版本号；config.json 损坏时的备份与恢复
5. **Linux 打磨**：portal 残留 cleanup 仅 KDE 下执行（GNOME 消除噪音日志）；
   无托盘桌面（GNOME 等）提供替代「退出」入口；.desktop + StartupWMClass
   系统图标注册（部分已验证：appIcon 替换已解决系统 H 图标问题）
6. **正式打包**：tauri bundle 未启用（bundle.active=false）——1.x 后期补
   Windows MSI/NSIS、Linux AppImage/deb
7. **跨平台回归核对**：多显示器 / DPI（Windows force-device-scale-factor=1）、
   开机自启行为（Linux .desktop vs Windows Run 键）、Alt+数字 切换
8. **GNOME / 其它 Wayland 实测**：portal 接口检查命令、绑定对话框、托盘扩展
   （KGA 为 KDE 专属通道；portal 为跨桌面备用）

## v2.0.0 —— 快捷脚本（Scripts）

### 需求要点（2026-09-06 确认）

- 除 web 应用外，可**绑定脚本 + 快捷键**：按热键 → 执行外部脚本/命令
- 设置中新增**「我的脚本」**页（管理脚本列表）
- **未来**：脚本会以某种形式挂到面板的**自定义按钮**上（按钮形态未定）——
  架构必须提前为这一步铺路，避免到时返工

### 架构设计要点（提前考虑项）

**核心抽象：把「可绑热键的对象」从 web 应用泛化为「动作（Action）」**

```
动作 Action { id, name, icon?, hotkey?, kind }
  ├─ kind = WebApp   → 现有 web 应用（激活 iframe 展示）
  ├─ kind = Script   → 新增脚本（执行外部命令）
  └─ kind = Scene?   → 3.0 预留（动作表天然容纳场景级实体）
```

- **数据模型**：config.json 顶层加平级数组 `scripts: [{id,name,command,shell?,cwd?,icon?,hotkey?}]`
  （与 `plugins` 并列，不混入插件数组——语义不同）。动作/热键相关的 UI 与后端
  逐步按「id → kind → 行为」路由，不写死 webapp
- **热键系统复用**：脚本动作走与 web 应用同一套——native 注册表 / KGA 动作集 /
  portal 绑定全部按 `desired_actions()` 泛化产出（script:id 动作描述如
  「运行 {name}」）；快捷键页列出脚本条目（名称/热键/删除），与 web 应用行并列
- **执行通道**：Rust 命令 `atray_run_script(id)`；spawn 平台语义待定——
  Windows（cmd /C or CreateProcess 无窗口）、Linux（sh -c 或直接 exec，
  继承桌面会话环境）；工作目录/输出/退出码 → toast 反馈；防重入与超时待定
- **面板按钮预留（关键）**：底部工具栏按钮 = **动作的视图层**，不是独立实体。
  现在按钮由「主面板 + 各 web 应用」静态派生；重构为「渲染动作列表 + 视图配置
  （显示哪些/顺序/图标）」。将来「自定义按钮」只是该视图配置的一种形态——把
  脚本/场景加进按钮列表即可，无需新实体。UI 与 IPC 的边界按此切，避免
  2.x 后期为按钮形态改动动作模型
- **安全与边界**：脚本可执行任意命令 = 桌面会话权限；2.0 先不做权限确认弹窗
  （个人工具），但接口预留 `confirm` 字段位

### 里程碑建议

- **2.0 alpha**：config 模型（scripts[]）+ 执行通道 + 「我的脚本」设置页
  （添加/列表/删除/热键绑定）+ 快捷键页脚本条目 + 分发路由（native 先行）
- **2.0 beta**：Linux KGA/portal 动作同步扩展 + 执行反馈（输出/失败 toast）+
  图标与顺序细节
- **2.0.0**：双平台打磨 + 文档；面板自定义按钮（形态确认后）作为 2.1/3.0 入口

## v3.0.0 —— 场景（Scene）

### 需求要点（2026-09-06 确认，原 2.0 规划整体后移）

- 场景 = 一组 web 应用窗口**并行**运行的工作区；用户可按场景整体呼出/切换
- **场景与单 web 应用是并列的概念**：顶层可切换对象既可以是单个应用，也可以是
  一个场景（同级实体，不是层级归属）
- v1 形态（单覆盖层、单活动应用、Alt+数字 切换）保留为「单应用」语义
- 与 2.0 的动作模型衔接：场景可作为动作表的一员（kind=Scene），热键/按钮机制复用

### 候选设计方向（3.0 设计阶段细化）

- **窗口模型**：并行 = 多 WebView 窗口平铺/分屏，还是覆盖层内多面板？需先定
- **配置模型**：config schema（scenes 列表：应用/热键/布局），与 scripts[] 并列
- **快捷键模型**：场景级热键（一键拉起整场景）与现有应用级热键并存
- **平台差异**：Windows / Linux(KDE Wayland) 的并行窗口、全屏、托盘语义；
  Linux 多窗口下全局热键按窗口 or 按场景注册

### 里程碑建议

- **3.0 alpha**：配置模型 + 场景管理 UI（CRUD / 排序 / 改名）
- **3.0 beta**：并行窗口/布局 + 场景热键切换
- **3.0.0**：持久化、双平台差异打磨、文档

## 版本速览

| 版本 | 内容 | 状态 |
|---|---|---|
| v1.0.0 | 从 anm-tauri 分出；覆盖层 / 设置 v2 / 布局 / 图标 | 历史 |
| v1.1.0 | Linux Wayland 支持（portal + KGA 雏形、显式全屏、残留清理） | 历史 |
| v1.2.0 | Linux 热键全链路打通（KGA SetPresent 根因修复）+ 对账 v2 + 细节打磨 | ✅ 当前可用 |
| v1.x | 打磨（见上表） | 规划中 |
| v2.0.0 | 快捷脚本（我的脚本 / 热键执行 / 动作模型 + 面板按钮预留） | 规划中 |
| v3.0.0 | 场景（Scene）：多 web 应用窗口并行 | 规划中 |
